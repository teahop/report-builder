# Referral vertical-slice evals

- `test_acceptance_source_only.py` — Cases 001/005 input-sufficiency (cached ledgers only).
- `run_smoke.py` — exactly one synthetic model smoke; hard stop afterward.
- `contamination.py` — refuse `example-reports/` paths.

Held-back final reports are never fixtures, prompt examples, or expected-output keys.
